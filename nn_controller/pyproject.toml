[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "nn_controller"
requires-python = ">=3.12"
dynamic = ["version"]

[tool.maturin]
features = ["pyo3/extension-module"]
